# changelog —— chemkit 版本历史

**版本号口径**：chemkit 处于 0.x 前期阶段，API 语义仍在打磨——版本号自
始应记为 0.3.x（0.3.1/0.3.3/0.3.4/0.3.5），早期 README 误记为 3.x，
自 0.3.6 起统一修正为 0.3.x 口径。

**升级纪律**：0.5.0 的门槛是**求解算法的实质飞跃**（性能、泛用性、鲁棒性
三者同时提升），不是功能累积。门槛未过则停留在 0.4.x，逐版交付审计与
根因修复；已落地的部分成果（守恒重构、根因修复）不回退，但不改版本号。
未完成项与下一步规格见 rchitecture.md §7。

---

0.5.0（在研，**未发布**）—— 守恒地基 + 走步过程审计：
按升级纪律停留 0.4.x 口径（版本号文件仍为 0.4.4，算法飞跃未完成）。
两个月的成果分两批：

- **精确酸碱地基的族定序缺陷**（`acidbase.py`）：pKa 条目的定义是
  `A ⇌ B + H⁺`，但 `build_families` 化边时丢掉方向、`dist_charge` 一律按
  "质子化态/H⁺"累加——对**标记酸恰是少质子一侧**的条目（H₃BO₃/[B(OH)₄]⁻、
  Tl⁺/TlOH、CO₂/HCO₃⁻…）整族分布取反。实测 pH 7 下硼酸根/硼酸 = 174
  （真值 0.0058）、TlCl 饱和液被算成 pH 1.87（真值 7.0）。修法：保留边
  方向 → 逐级符号 `sgn_k`（同向 +1、反向 −1）。验收：族分布复现自身 Ka
  **78/80 → 80/80**，**12/12 教科书锚点**（HCl 0.00 / NaOH 14.00 /
  NaAc(1M) 9.38 / NH₄Cl 5.12 / NH₃ 11.12 / NaHCO₃ 8.34 / Na₂CO₃ 11.62 /
  H₃BO₃ 5.12 / NaHSO₃ 4.50 / NaH₂PO₄ 4.65 / Na₂HPO₄ 9.75 / TlCl 6.98），
  T01 ΔpH −5.132 → −0.018。
  **新增常驻检查** `testsuit.acidbase_check`（全库逐条 + 锚点，92 条断言；
  断言总数 1202 → 1294）——这类缺陷第一次有测试兜底。
  **重要连带**：`architecture.md` §7 F 当年"证伪 pH 机器替换"用的参照物
  正是这个带缺陷的 `charge_pH`，该结论须在修好的参照物上复核，
  pH 闭环入联立求解器的可行性判断暂时悬置。
- **净方程装配器守恒重构**（`equations.py`）：`_build_equations` 里六个
  各自为政的化简启发式（质子噪声对消、水解对消、痕量过滤×2、共轭对
  对齐、H/O 补配平）此前每个都能破坏守恒、容差是 3% 相对值——2.3% 的
  Fe(OH)₃ 被删掉照样通过并被固化成 4 条测试标准。现在化简只是**候选
  生成器**，唯一裁决在闸门 `_drop_budget_ok`：呈现的方程自身必须在
  **非 H/O 元素 + 电荷**上守恒到呈现分辨率 `max(1e-5, 5e-4×尺度)`
  （H/O 交给溶剂水）。美化接受条件同步重设计（物种数不是"是否干净"的
  判据——N09 的 7 物种教科书式曾被拒），整数化改用精确整数算术。
- **标准修正 8 条**（T02/U09/21/N30/D32/NR95/Y14/U10）：期望方程原本
  元素/电荷不守恒（99:52 族 Fe 52≠51、电荷 +57≠+54），是旧美化路径的
  产物被固化；`tools/eqcheck.py` 精确有理核验，原值留档于 `note`。
- **T25/H77 的 `1.001ClO^-`、`0CO3^{2-}` 类呈现缺陷**修复（显示地板）。
- **N15 `1.999CO_2` 类**：净差向量的 H/O 由溶剂水承担，浮点档补 H₂O
  必然连带补 H⁺（超契约容差）⟹ 呈现退化成既非整数又缺水的浮点式。
  新增 `_stoichiometric_net`：**先整数化（非 H/O 元素 + 电荷精确守恒，
  化学方程式的硬条件）再补 H₂O**，仍是候选、仍过闸门。
- **`_buffer_titration` 记账守恒重构**（architecture §7 L 规格落地）：
  堆只做强度定序、量一律从活账本读、每步增量移动；pH 从滴定后的账本
  导出（分布由守恒决定、pH 由分布决定，不再共用一个变量）。族总量
  破例 E42/K02 5→0、B19 77→0、B27 126→0、N15 276→0、T84 362→0。
  同批修掉 heapify 缺失（强度序退化成账本序 → 幻影碱）与碎屑平台对
  （he 残 1e-17 时拿微量产物算 Henderson → pH 钳位 −1）。
- **新增审计工具**：`tools/titr.py`（逐调用族总量对账）、`tools/phlog.py`
  （pH 估计轨迹：同账本不同 pH 即缓存/堆序缺陷）、`tools/osc.py`
  （走步反复逼近画像：重复签名/锯齿/步数分布）、`tools/eqcheck.py`、
  `tools/hess_audit.py`（常驻，H46 病灶即它查出：派生候选 logK 必须与
  基候选线性组合恒等）、`tools/hydrate_audit.py`、`tools/diff*.py` 等。
- **走步过程审计**（architecture §7 M）：全量 1173 例中 **266 例（22.7%）
  带"反复逼近"签名**、58 例 >50 步、最差 240 步 / 单净键重复 37 次。
  触发点④（净键重复即联立）实验两个方向均失败并已回退入档：不限次 →
  跳步与走步互拆（T99 47→1280 步）；限次 → 5 例语义翻案。结论：要么
  联立覆盖整个活性社区，要么先把 pH 闭环做成精确质子条件——这是 0.5.0
  门槛（算法飞跃）的真正入口。
- **断言锁化学、不锁轨迹**（本轮，architecture §7 W）：先修口径再拿性能。
  新增 `tools/fragility.py`——把投料做 `(1±1e-11)/(1±1e-9)` 相对扰动
  （1 mol 差 1 nmol，物理上无意义）重跑全库断言，**仍翻红者 = 锁了轨迹**。
  实测全库只有 **2 例**（P11、N19，都是"要求某条中间步骤逐字出现"），
  其余 40 例 digest 位移**不触及任何断言**——于是"42 例语义位移"再也不能
  当作拒绝性能改动的理由。修法两条：① `eq_has` 改为**张成判据**
  （`testsuit._step_spanned`：精确有理数下解 `Σcᵢ·stepᵢ = need`，非负系数
  即可，附规范方向 `H⁺+OH⁻→H₂O` 作自由列）——同一条净变换经不同中间体
  合法到达不再被误杀，少原子/电荷不平/需倒走照样拦住；② 三条标准改写：
  P11 删路线断言（净方程 + `PbCO₃≥0.9` 已覆盖化学）、N19 同上、
  Z31 从"精确整数配比"改为 `HSiO₃⁻∈[0.2,0.4]` + `H₂SiO₃≥0.3`
  （凝胶化程度是连续量，整数配比把它固化）。修后脆弱性审计 **0 例**。
- **求根加速落地**：二分收敛容差 `1e-11·max(1,x_max)`（原本跑到浮点饱和
  ~52 次，后 ~15 次空转；容差比任何消费 `ext` 的判据小 5 个数量级）。
  `S_of` 调用 −17~−18%（Hg2_1 21734→17976、Cu31 123045→100359），
  全量断言 **1294/1294**。轨迹位移 42 例多为**收敛变好**（Z31 resid
  0.02→0、N23 8.28→0、H80 1.12→0、Y03 10.2→1.83、F21 0.63→0.02）；
  新基线 `digest_all = 088a6a368c125b0c`，`resid_max` 10.213→8.073、
  `n(|S|>1)` 64→59。**方法论**：digest 位移只作辅助复核，不作门槛。
- **数据修正**：`ksp.json` 的 CaSO₃ `pKsp` 7.2 → **6.5**（CRC 99th ed.
  p.5-188：Ksp 3.1e-7；室温稳定相是水合物，原值把亚稳无水相当室温相）。
- **求根加速的第二道门：根选定**（§7 X，本轮）。W-5 之后最大杠杆是二分
  本身（全量 `S_of` 约六成花在 17381 个括号 × ~31 次求值上）；新增
  `tools/roots.py` + `engine.ROOT_AUDIT`（默认 None，实测关闭时不改任何
  语义：digest/sof/iters 逐项一致）在同一括号上复算 Illinois：
  **2.04×** 更少求值（31.3 → 15.3 次）、网格多根 1.30%，但**2.6% 的括号
  两法根差 >1e-6**。形状诊断（61 点细扫）显示 f 是"**−/+ 口袋**"结构：
  二分返回**最左根**（= 反应第一次 S=0 的停止点，唯一正确），Illinois
  会跳进口袋上方的正区收敛到**第二个穿过点** ⟹ **过执行**（T50 型
  0.0156→0.0271）。**判否：不是断言锁轨迹，是求根法本身错。**
  根因归到 pH 机器：**四分支启发式切换让 pH(x) 不连续，S(x) 因此长出
  伪口袋**——"近似掩盖问题"的又一例，也是下一轮的方向（让 pH 闭环来自
  同一套平衡条件）。**已证实**：把 f 与 pH(x) 对齐细扫，口袋逐例落在
  pH 跳变上（T50 pH 9.51→5.81 一跳 3.70；RX13 4.69→11.02 一跳 6.33）。
  全库普查（142 个带轨迹括号）：**23.2% 的括号 pH(x) 相邻点跳变 >1.0
  单位、20.4% >3.0，最大 9.10**（Sb(OH)₃⇌[Sb(OH)₄]⁻），集中的全是
  两性/固相族（Al/Ga/Sb 的 ⇌[M(OH)₄]⁻、Cu(OH)₂⇌Cu²⁺）。pH 不连续
  不是个别怪癖而是**两性/固相族的通病**，同时解释了 §7 T 的螺旋慢例
  （走步在悬崖两侧来回）与本次求根加速被挡。顺带记下 5/435 括号
  `f(0)≤0`（候选挑选 pH 与探针重算 pH 不一致）待查。
- **连续化原型：方向验证 + 判否理由**（§7 X-6，本轮）。`charge_pH` 新增
  `pinned=(z,pKsp,x,y)`（固相 `M_x(OH)_y` 在场时自由阳离子被溶度积钉住，
  电荷项随 pH **连续**变化），`tools/phcont.py` 猴补接入 `estimate_pH`
  做全量实测：**换根 2.6% → 0.0%**、pH 跳变 >3.0 的 20.4% → **1.9%**
  （max 9.10→7.06）——口袋那道门确实开了；但断言 1173 → 1138（红 35）。
  **判否不上生产，理由不是翻红而是语义错位**：钉住描述的是平衡态，
  而二分探针评的是中间态（账本里的自由离子量就是该步的程度 x，替换成
  饱和值等于把 x 从电荷平衡里抽走）。真正买到的是三件事：① 方向对
  （收益真实）；② 残余跳变（4.9–7.1）全在"钉住档 ↔ 回退原机器"的**接缝**
  上 ⟹ 只补固相不可能连续，必须整段同一方程；③ 缺口定位到**数据层**：
  金属水解梯 `M^z+ ⇌ MOH^(z−1)+ + H⁺` 不在库里（`beta.json` 只有两性
  端元 `[M(OH)₄]`，无单羟中间体），无固相区间无数据可用，只能退回
  `Kh = Ksp/Kw^y` 近似。⟹ 0.5.x 主线：补水解梯数据 + 整段一套方程。
- **跳变逐条归因 + "直接换精确解"实测判否**（§7 X-7，本轮）。审计给每个
  采样点补"走了哪条返回路径"（`speciation.PH_TAGS`，仅审计时启用）与
  探头态电荷自洽残差。归因结果：最大跳变（6–7 单位）几乎全在
  **`酸侧max → OH⁻直读`**——`_buffer_titration` 残余 He 跨过硬阈值
  `|He|≥1e-3` 时两侧公式相差 6–7 单位（4.7 vs 11.0），是**公式在阈值处
  不连续**；T50 的 3.70 则是同分支内 Henderson 换缓冲对。
  自然修法"直接解统一方程"实测判否：断言 1117/1173（红 56）、pH 跳变
  >1.0 从 23.2% **涨到 71.9%**。原因把路线钉死了：**精确电荷平衡只对
  账本里已有的物种成立**，金属离子在账本里是固定电荷（无水解梯、无配体
  物种），0.01 M CuCl₂ 的精确解是 pH 7（看不见 Cu²⁺ 的水解酸性），而
  机器的 `Kh=Ksp/Kw^y` 近似虽粗却给对酸侧 pH——**机器是在补数据的缺**。
  副产物：精确解下探头态电荷自洽 0/24583 超标（基线 2.6%）。
  ⟹ 结论：**数据先行**，数据不全时统一方程在金属体系上是病态的。
- **两次"就地抹平阈值"的尝试入档 + X-5 反常归因**（§7 X-8，本轮）。
  ① 把残余强/弱酸碱并入分支 4 的"取最大"（阈值处天然连续，还修掉
  "1e-3 M 强碱被两性中点式盖过"）实测 **1280/1294（红 14）**：Al(OH)₃
  两性溶解丢失、PbCl₂ 溶解度 0.0015、Rh(OH)₃ 不溶于酸、铁在中性水中
  "反应"。根因是残余 He 在**未计金属水解**的前提下算出（水解在分支 4
  的 Kh 角色里）⟹ 偏大，提升进 max 就压过真实贡献；两支路要一致只能
  一起解（即统一方程，被数据卡住）⟹ 回退。
  ② 5/435 括号 `f(0)≤0` 的反常查清：**全是 redox 候选**，走步评估用
  真实账本、探头用虚拟账本（led_v）⟹ 同一反应两个求值点不同态（闭式
  快通道假设已先被排除：两侧 0/5）。反向改实测 **1289/1294**：E14/D14
  是标准锁旧路线（新叙述 `Cl₂+OH⁻→Cl⁻+HClO` 才是氯水规范步），但
  I24/KIN02/KIN06 **真回退**——恰好印证虚拟账本的设计意图（pH 一致的
  自由形态参与氧化还原竞争）。⟹ 回退，把"哪个账本对 redox 有权威"
  记为待决设计（守恒闸门作仲裁），不就地方向糊上。
- **落地：redox 驱动力两侧同态**（§7 X-9，本轮）。判据是不变量而非口味：
  **同一反应的驱动力不能因为"谁在求值"而变**。既然探头侧的虚拟账本是
  设计意图（上条三例真回退证明），就把候选评估侧也对齐——redox 候选
  用 `_virt_led`（与落实路径同一守恒闸门 `_swap_conserves` 放行才用），
  且 `logc` 缓存不跨账本复用（第一版漏这条 → 21 例翻红；补上即全绿）。
  验收：**f(0)≤0 的非法括号 5/435 → 0/431**（不变量恒等成立）、
  换根 2.6%→0.9%、pH 跳变 >3.0 的 20.4%→12.8%、探头态电荷不自洽
  2.6%→0.8%、残差 p90 0.033→0.024、n(|S|>1) 59→55、S_of 调用 −0.3%
  （守恒闸门几乎免费）；全量 1294/1294、脆弱性审计 0 翻红。
  这是根因修复：不连续的 pH 仍在（数据缺口未补），但"选择与执行不同态"
  这条结构性不一致消除了——它同时是 X-5 反常、部分口袋与部分电荷
  不自洽的共同来源。基线随之重采 `digest_all = 213a116113124199`。
- **张力例归因 + 残差口径修正**（§7 X-10，本轮）。新增 `tools/tension.py`
  把 `resid_live` 的罪魁按旗标归因，并给引擎补两处只读标注：双向禁用的
  `dis_why`（原因/|S|/ext/x_max）与探针的 `pH_solver`/`S_pres`。归因结果：
  榜首一簇（8.07 / 6.18×4 / 6.11 / 5.75 …）全是 `2H⁺+Fe→H₂+Fe²⁺` 一类
  耗酸 redox，且全判"零推进"。查证（Fe+水，含逐迭代 trace）：第 1 步
  S=+14.88 执行 5e-4；第 2 步同候选 S=+6.18 却解出 0 —— 因为
  **estimate_pH 在 He 跨 −1e-3 时从 7.0 跳到 11.0**，耗 H⁺ 反应一推进
  pH 就跳，H⁺/H₂ 电位跌到 Fe²⁺/Fe 之下、S 转负 ⟹ 二分把根挤到 x<1e-4、
  微步机制正确禁用。**walk 自洽，残余 S 是"pH 反馈前"的驱动力**，由阈值
  不连续制造。这是第三个独立证据指向同一病灶（前两个：X-4 的悬崖挡住
  Illinois、X-6/X-7 的统一方程失败）。**残差口径修正**：S 改在求解器
  自己的 pH 上评（呈现 pH 值单列 `S_pres`）——旧口径拿呈现 pH 评残差等于
  换个态质问求解器。实测 `n(|S|>1)` 59→**35**、`resid_p90` 0.024→**0.008**、
  `resid_max` 不变；基线按新口径重采（digest 不变，证明标注行为中性）。
- **He 阈值第三次尝试（判据标尺）实测同 14 例 + 张力普查定位"耦合写法"缺陷**
  （§7 X-11/X-12，本轮）。① 把残余 He 并入 h_c/o_c（阈值连续）但两性/缓冲对
  的触发仍按物种贡献比较 ⟹ **失败例与 X-8 逐例相同**（1280/1294）——坏的
  不是标尺是值本身。三次尝试共同说明：|He|<1e-3 的残余**既是幻影也可能是
  真实自由酸碱，机器在调用点无法区分**（它不知道态是否已平衡），这正是
  统一方程才能回答的问题。② `tools/tension.py` 加 `--census/--class` 后
  489 例张力分成：406（83.0%）已达平衡（最大 |S| 0.096，零头）、62（12.7%）
  零推进（X-10 的 He 阈值簇）、**20（4.1%）无旗标却没走**、1（0.2%）限幅。
  ③ 那 20 例逐条查证（T34 FeCl₃+3KSCN）：排空循环连续挑中 8 条 **H⁺ 耦合
  写法**（`[Fe(SCN)₃]+3H⁺→Fe³⁺+3HSCN` 等，S 高达 15.98），它们全被酸储备
  顶死或根被 pH 悬崖挤到 0，idle 达 8 退出；而**同化学的未耦合写法**
  `[Fe(SCN)₃]→Fe³⁺+3SCN⁻`（S=3.649、双向在场、无任何旗标）x*=0.0121 ≫ 微步阈
  9.8e-5，按 S 排序永远轮不到。⟹ **候选身份（netkey）不按质子规范归一**：
  同一化学的多种写法该共享身份，仲裁该挑"能动的写法"而不是让 S 最大者把
  化学饿死。④ 两次修法实测均判否：只豁免"限幅"→**行为完全不变**（digest/
  sof/iters 逐项相同）；"零推进"也豁免→walk 确实执行了等价写法但**来回乒乓**
  ⟹ **1291/1294**（Co32/Co33 钴氨、J14 PbCl₂ 溶解度回退）⟹ 回退。
  ⑤ 下一步主攻：把 `_netkey`/freeze/disabled/hist 的身份**按质子规范归一**
  （§7 O-5 已在测试比较器落地，这一步推广到引擎内部候选身份）。
- **全库候选一致性审计：pKw 约定双轨**（§7 X-13，本轮）。新增
  `tools/cand_audit.py` 做**不变量**检查（同一净变换正反候选必须
  `logK(正)+logK(反) == 0`，精确无容差）：1142 个初始态 / 14711 条候选里
  **230 例不正反相消**，差额恒为 **−0.0167**（4H⁺ 反应），另有 13 例同净键
  重复且 logK 不一致。根因：`pKw_of(298.15) = 14.004172` 而 `PKW_298 = 14.0`
  且**全部数据表以 14.0 为参照**——Hess 派生通道经 `pkw_coeff` 用 14.004172，
  β/Ksp 直连通道把 14.0 直接烘焙，两条路径相差 `coeff×0.004172`。
  **一行根因修复已实测**（把经验式锚定到数据约定，298.15 K 恰好 14.0，
  其他温度平移 0.0042）：**不变量 230 → 0**、重复例消失；代价是全量
  1294 → **1270**，24 例翻转。按用户指示"以化学事实为准"逐条裁决：
  ① "不反应"族 17 例——终态**恰好落在 Ksp 线上**（1 M CuSO₄ 自水解到
  pH 4.15 时 Q = 10^-19.7 = Ksp，析出 5e-4 mol Cu(OH)₂ 化学正确），标准的
  `eq: null` 属呈现约定；② 铝/镓溶液酸性族 8 例——**同一类痕量析出却要求
  叙述该方程**，与①互相矛盾（且真实机理是水解，属 §7 X-7 的数据缺口）；
  ③ 边界族 5 例（1:3 恰在两性边界、T64 的 S²⁻ 水解比例等）需单独裁决。
  ⟹ 锚定是正确的根因修复，但落地前须先按化学事实改写这 24 条"由约定
  而非化学决定"的断言。本轮只交付工具与诊断，生产路径不动（1294/1294）。
- **方程式层重构：先结构化、后渲染 + 两版净方程 + 可逆符号**（§7 X-16，本轮）。
  用户口径：精编方程式必须体现**有意义的平衡过程**；可逆过程用可逆符号；
  **不预先转换**成字符串（`Reaction` 只存结构，读取时才渲染）；区分 **TeX
  模式**（默认，进 Markdown `$$` 块即可显示）与**纯字符串 fallback**
  （手动 `.plain()`，连上下标标注都不用）；不留 alias；同步改测试用例库。
  落地：`@dataclass Equation{left,right,reversible,kind,extent}` +
  `_normalize_equation()`（只产系数结构）/`render_equation(..., mode)`（只拼
  字符串）；`reversible` 布尔由引擎 `degree` 决定（完全 ⟹ `->`，未完成 ⟹
  ASCII `<=>`，避免 U+21CC 在 GBK 控制台打印炸掉）；精编口径修正——痕量
  **新相**不再抑制（饱和线痕量析出是**有意义的平衡过程**，讲成可逆平衡而
  非"反应发生"），只保留"痕量配合物"抑制。API 自测改为结构断言，失败记录
  写 `.plain()` 文本。验收：全量 **1294/1294**，`digest_all` 不变
  （213a116113124199 ⟹ 纯呈现层重构、判定语义零位移）。
- **pKw 锚定落地门槛：断言从"锁阈值"改为"锁判定"**（§7 X-17，本轮）。
  锚定实测不变量 230→0，但全量 1294→1269（25 例翻转）。逐条按化学事实裁决：
  ① **痕量平衡叙述 19 例**（NR18/19/24/48/49/50/51/53/97/107/111/133/153/168、
  V01/V04/V05/V20/V21）——终态恰在氢氧化物 Ksp 线上，锚定后引擎叙述一条
  **痕量可逆平衡**（`2OH⁻+M²⁺ ⇌ M(OH)₂`，0.05% 投料、changed=False）；判定层
  在两种约定下一致，翻转的只是**随阈值出现/消失**的那条叙述 ⟹ 删去这些用例
  的 `eq` 断言（保留判定层与量级断言，`net_equation_raw` 仍可查），改后
  **带/不带锚定都通过**（库对两种约定鲁棒）；② **路线锁定 16/E55**——`eq_has`
  要求 [Al(OH)₄]⁻ 中间体，而引擎按瞬态中间体塌缩并成直接沉淀（1:3 投料的
  化学事实就是全部 Al(OH)₃）⟹ 按 §7 W 删去，净方程与产量断言保留；
  ③ **待裁决 8 例**（21 AgBr+氨水的同侧共轭对未对消、T64 水解配比、
  NR57/NR152 痕量腐蚀踩显著线、NR153 的 Cu₂O/pH 依赖、Co33 的 β6/Ksp
  竞争、EU01 电对、RX12 痕量回溶）——§7 X-17 已逐条给出化学判词与下一步，
  锚定保持 **staged**，待这 8 例清零后一次性落地。
- **净方程的过程级归属：四种设计实测，正解定为 H⁺ 通量归属**（§7 X-19，本轮）。
  21 号真因是"同一份质子在净方程里写两次"（`NH₃+H⁺→NH₄⁺` 步 + dHe 的碱性重写）。
  呈现层四种归属设计全部全量实测并判否：① 同侧 `HA+OH⁻→A⁻+H₂O` 无条件中和
  → **1210/1294**（84 例水解/中和被自身抵消）；② 加"共轭碱已有存量"约束 → 9 例
  （21 号修好，但残余仍误伤正当水解）；③ 只聚合"化学反应步"（B4 逐步标记）
  → **1213/1294**（弱酸参与的反应被拆散：`2CH₃COOH+Mg→2CH₃COO⁻+H₂+Mg²⁺`
  退化成 `2H₂O+Mg→H₂+Mg(OH)₂`）；④ 再加"强酸碱投料不归属 + ≥5% 过滤"
  → 21 号与 H81/T89/W22 都对，但同类退化仍在。**关键认识：弱酸解离与氨水解
  形式相同，区别在质子的去向**（是否被反应子网络内的其它步消耗）⟹ 正解是
  **H⁺ 通量归属**（步表配对供/耗质子步、递归圈出投料主导的反应子网络），
  下一轮实现。本轮交付引擎新公开字段 **`step["chem"]`**（B4 判据逐步版；判据用
  **反应物自身来源集**——`origins[H_ION]` 会被所有触碰质子账本的步污染、
  把氨水解误判为跨投料反应，实测）——行为中性（1294/1294、digest 不变）。
  **【后续更正】**：上述"正解"前提已被推翻——21 号膨胀的真因不在质子归属，
  而在美化入口**回读 TeX 渲染串**（见下条 / §7 X-20）；H⁺ 通量归属的朴素实现
  也补做了全量实测：**1216/1294**（78 例退化、零收益）⟹ 一并判否。

- **根因修复（呈现层）：判据回读渲染串 ⟹ 静默失效**。`_build_equations` 的
  "大系数美化"入口是 `"." in net_str or _max_coef(net_str) > 20`，而 `net_str`
  自 v0.5.0 起是 **TeX**（`\mathrm{…} \rightarrow …`）：`_max_coef` 走
  `_parse_equation` 的箭头表（`->`/`<=>`/`→`/`⇌`/`=`），TeX 的 `\rightarrow`
  一个都不匹配 ⟹ 返回 0.0 ⟹ **"系数 >20 也要美化"这条分支自 v0.5.0 起死掉**，
  只剩"串里带小数点"能进美化。实测链条（21 AgBr+浓氨水）：未锚定 pKw 时
  规整成 `4.278NH₃ + 1.639AgBr + H₂O → …`（带小数点 ⟹ 进美化 ⟹ 教科书式
  `2NH₃+AgBr ⇌ [Ag(NH₃)₂]⁺+Br⁻`）；锚定后规整成 `34NH₃ + 13AgBr + 8H₂O →
  13Br⁻ + 13[Ag(NH₃)₂]⁺ + 8NH₄⁺ + 8OH⁻`（整数、无小数点 + `_max_coef` 恒 0
  ⟹ 漏出）——**同一条化学结论随"规整是否恰好产生小数点"出现/消失**。
  修法：新增 `_coef_stats(*sides)` 从**结构**取 `(最大系数, 是否全整数)`，
  入口与接受判据都用结构判定；删除死函数 `_max_coef`。
  验收：未锚定 **1294/1294**、`digest_all` = `213a116113124199` **不变**、
  `iters_total`/`sof_total` = 12144/844991 **不变**（确定性零位移）；
  锚定 **1269 → 1289/1294**（21 号在两种 pKw 约定下都给教科书式）。
  **制度化教训**：渲染模式一改，"把渲染串回读成数据"的判据会**静默失效**——
  判据一律看结构。修后审计：库内已无 TeX 串回读路径（`_parse_equation` 只吃
  引擎 ASCII 步式与测试标准；`_step_to_ionic` 的去重键走 plain 模式）。
- **标准重裁 2 条（锚定/未锚定都过）**：E55 明矾+适量NaOH 去**路线锁定**
  （要求痕量铝酸根中间体两条；实测 4.5e-4 → 3e-6 随 Kw 约定移动，锁它=锁轨迹，
  同 16 号处置；净方程 + `Al(OH)₃ ≥0.8` 保留）；T64 H₂S+2NaOH 删 `eq`
  改 `has_range`（pKa2(H₂S)=14.0 ≈ pKw ⟹ Kb(S²⁻)≈1，组成对 Kw 约定敏感：
  S²⁻/HS⁻ = 0.384/0.616（≈4:6，与用例自己的注记一致）↔ 0.5/0.5，
  整数比 2:1 ↔ 1:1 只是**规整渲染**的产物，不是化学结论）。
- **下一轮主攻已定位（本轮实测，architecture §7 X-21）：自催化循环缺"净反应
  驱动闸"**。NR57 Pb+ZnSO₄ 锚定后跑出 **97% 转化**（Pb 0.971 mol + Zn²⁺ + SO₄²⁻
  + 2H₂O → PbSO₄ + Zn(OH)₂ + H₂）：步表是「饱和线转移 `Zn²⁺→Zn(OH)₂+2H⁺`
  （logK −11.0，S=0.0，限幅驱动）」与「`2H⁺+Pb+SO₄²⁻→H₂+PbSO₄`（logK 12.1，
  S=+3.08）」交替自催化——后者的 S 用的是前者刚放出的 H⁺ 造成的瞬时酸度。
  引擎自身数据给出净反应 logK = 12.1 − 11.0 = **+1.1**（K≈12）⟹ 平衡应停在
  `[Zn²⁺][SO₄²⁻] ≈ 0.08`（x≈0.72），实际跑到 **8.3e-4（超出净平衡 ~100 倍）**。
  逐条 S 检查无法约束**循环**的净驱动 —— 这是 0.5.x 的一个新缺陷类。
- **锚定剩余门槛 5 例**：NR57（上条）、NR152 Fe+FeSO₄（1.15% 痕量腐蚀，
  同源 H₂ 通道）、Co33（β6(Co²⁺-NH₃) vs Ksp(Co(OH)₂) 数据审计）、
  EU01（Eu³⁺/Eu²⁺ 电对：产物里是 Eu(OH)₃ 不是 Eu²⁺，查入表与闸门）、
  RX12（Fe 产率 0.0845 vs ≥0.09，与 NR152 同源回溶）。

0.4.4 —— **数据审查版**（数据库逐条化学事实审查，**判定语义零改动**）：
- **样例库去重**：1218 → 1173 例（同一实验的多条记录合并，取更严的
  断言；6 处冲突单独标记）；`tools/dedupe_tests.py`。
- **逐条审查第一批**：1 处引擎算法缺陷 + 8 处数据缺陷。`_ksp_xy` 用阳
  离子**元素计数**推化学计量 ⟹ 多核阳离子（Hg₂²⁺）翻倍，8 条汞(I)族
  Ksp 指数错；改为元素空间精确解 `solid + w·H₂O == x·cat + y·an`。
  另修 3 处配对写错（K₂Na₂[Fe(CN)₆] 应为 K₄[Fe(CN)₆] 等）。
- **无水盐 / 水合物维度**（V16 真因）：Ksp 的**测定相**与账本写法可能
  不一致，`ksp.json` 新增 `phase` 字段**声明**（只声明、不改数值——
  换相是化学决策）；`tools/hydrate_audit.py` 做边界敏感性扫描，全库
  221 处固相析出量 <20% 投料（±0.5 pKsp 即可让其消失），其中 5 处 ⚠
  标注为"选相会翻转结论"的实例。
- **数据表清理**：`thermo.json` 删 5 条死键、裸 `Cl_2` 明确为水相、
  补 `Cl_2(g)`；`couples.json` 删重复的 (H₂SO₄, SO₂) 条目（隐藏闸门）、
  给 E0=1.15 条目补显式 `closed_with_red: ["Cl^-"]`；`pka.json` 的
  `H_2SiF_6` 级数 1→2；`beta.json` 165→158（7 条精确重复合并）。

---

0.4.3 —— **基线审计版**（0.5.0 立项前的勘查，**不改变任何判定语义**）：
不升 0.5.0 —— 求解算法的实质飞跃尚未完成，按纪律停留 0.4.x。本版
交付一次系统性审计 + 一块精确酸碱地基，把 0.5.0 的施工顺序钉死：

- **审计一：测试基线不干净**。HEAD 实测 **1245/1247**（V16/Z31
  长期红），仓库内 `converg-baseline.json` 的 digest（`2b9c67b8`）与
  HEAD 代码（`dfdabe43fd1537a0`）不符——基线是更早工作树状态的
  快照。差分审计前必须重采，否则会把"与陈旧基线的差异"误读成语义
  位移。
- **审计二：电荷守恒不变量在 5.0% 用例上被破坏**（**最重要**）。
  引擎把游离质子记为单一带符号标量 `H_excess`，其名义含义是"账本
  净电荷的相反数"；逐例核对 `Σz·n(final) + H_excess` 发现 **61/1218
  例偏差 > 1e-3**，最大 **13 eq**（N09）、9 eq（E51）、6 eq（E12）——
  不是浮点尘埃而是结构性记账缺口（根因即 v0.4.1 第六次负结果查明的
  "He 拆源缺失"：投料酸质子与反应生成质子同账，而其反离子不总在
  账本里）。**任何以电荷守恒为立论基础的求解器都会在这 5% 上失效**
  ——这是 0.5.0 的第一道关。
- **审计三：数值路径变更会静默改变结果**。把 `joint.py` 里逐行重复
  的 `joint_solve`/`_solve_ph` 两段 Newton 主循环合并为单实现（数学
  逐语句等价，仅去重）后：断言仍 1245/1247 全绿，但 converg digest
  与 iters_total（13156→12961）双双漂移，7 例语义位移。**"全绿"不等于
  "没变"**——0.3.8 立"收敛质量基准"制度的价值实证。
- **新增 `chemkit.acidbase`**（未接入主路径，纯诊断/校验）：质子化族
  + 逐级 pKa 严格多级分布 + 电荷平衡闭式解 pH，13 项教科书对照全部
  命中（HCl 1.000 / NaOH 13.004 / NaAc 8.882 / NH₄Cl 5.125 /
  NaHCO₃ 8.350 / Na₂CO₃ 11.642…）。三个实现教训入档：族定序须用
  **最长路 + 逐级 pKa 单调递减**（"沿 acid/base 方向链走"会把碳酸梯
  pKa 错配一级）、n≥2 合并 pKa 条目是冗余边须剔除、族分布权重是
  `10^(Σ(pKa−pH))` 指数（符号写反会把分布倒过来）。
- **施工顺序（0.4.4 修订，旧稿第 ③ 条已被实测证伪）**：
  ①He 拆源与守恒修复 → ②重采 converg 基线 → ③**pH 连续化**
  （删掉 `|He|=1e-3` 二选一端点，残余 He 作为基线源 + 各弱源在 He
  背景下取精确解）→ ④**显式"反应完备性判据"**（连续化会同时拆掉
  痕量死区——它是 7 例"非反应"结论的真正依据，必须功能等价替换，
  不是可选优化）→ ⑤联立求解器（log 域 Newton + Gram-Schmidt 秩
  归约）重新接入。③④ 是同一改动的两面，必须一起交付。
  见 architecture.md §7「0.4.x 现状审计」E/F/G。
- **③ 的实现与代价已实测**（分支 `rework/ph-continuity`）：
  J06 悬崖 ΔpH 3.216 → 0.000（`tools/cliff.py` 可复现），退出 He
  1.000e-3 → 1.81e-6，J06 的 live 残差 **6.039 → 0.002**（真解），
  全量残差 p90 1.696 → 0.123（14×）、max|S|>1 的例 174 → 129。
  **代价：iters +5.5%、耗时 +25%、1247→1224（23 例翻红）**，
  且 **J14 冻死**（live 残差假性归零——frozen 由 0 涨到 16，
  溶解度 0.0015 M 与同物同温 J06 的 0.018 M 自相矛盾）。
  **判定：③ 不是净胜，停在分支上**，等 ④ 完备性判据与 ⑤ 秩归约
  一起验收（理由与实现规格见 architecture.md §7 G-1/G-2）。
- **指标陷阱入档**：`resid_live` 排除 frozen 平衡（既定语义），于是
  "把欠收敛平衡全部冻结"能让 live 残差变 0.000 而并未收敛。质量口径
  必须同时看 **live 残差 ∧ frozen_n 未涨 ∧ 跨例一致**。
- **H46 已解 —— `beta_ksp` 派生通道的一个 pKw 系数**（本轮，见
  architecture.md §7 H）。`candidates.build_derived` 的 `beta_ksp`
  通道把沉淀项的 pKw 系数取成 `charge_of(center)`（中心离子电荷），
  应为**固相分子式里的 OH 数 y**。两者只在 M(OH)_z 型相等；Ag₂O 是
  x=2/y=2/z=1，**少减一个 PKW_298 ⟹ logK 整偏 +14.000**。
  ```
  H46  max|S| 13.298 → 0.008   frozen 7 → 0   steps 364 → 7   pH 6.28 → 6.99
  N12  resid_live 4.717 → 0.0      Z17 0.121 → 0.0     H16/B14/Hi4/Hi5/21/22 → 0.0
  iters 13156 → 12651（−3.8%）；语义位移 12 例，全部银体系，无一变坏
  断言 1245/1247 不变
  ```
  H46 的标准注释早写明"严格平衡解：配合 0.70 / Ag⁺ 0.20 / Ag₂O 0.05 /
  pH 7.0"；修正后引擎给 **0.707 / 0.208 / 0.0425 / pH 6.99** —— 罕见
  的"标准对、引擎错"。
- **新工具 `tools/hess_audit.py` 常驻**：派生候选本就是基候选
  （beta/pKa/Ksp/水）的线性组合，故解精确有理方程组
  `Σaᵢ·vec(baseᵢ) = vec(d)` 并校验 `logK(d) = Σaᵢ·logK(baseᵢ)`。
  全量 12971 条：可解 12136，**不自洽正好 4 条，全是 `beta_ksp:…/Ag₂O`，
  Δ 逐一 +14.000**；修后归零。
- **"行秩亏"是伪诊断（重要教训）**：派生行与基行线性相关**正是它应当
  如此**（Hess 组合的定义），秩检查永远看不出问题；真病是**组合系数
  写错**让方程自相矛盾。旧文档把"Hess 派生行秩亏"当成需要
  Gram-Schmidt 秩归约的求解器难题并排进 ⑤ 重写清单——**实际是一行
  算术**。判据："线性相关导致求解器困难"必须先排除"这条线性组合
  本身算错了"。
- **U09 标准按化学事实修正**（`AgNO₃ 2 + NH₃ 2`，原 Ag₂O ≥ 0.5 →
  0.05、eq 改为配合主导）。两法一致给 0.0998：引擎走步 与 闭式解析
  （文献常数 pKsp 15.4/logβ₂ 7.2/pKa 9.25 → K = Kw²/(Ka²β₂Ksp) =
  10^-1.3 = 0.05；N 守恒代入得 `4s²/(1−s)² = 0.05` → s = 0.1006；
  同法算 H46 得 0.0428，引擎 0.0425。Ksp 反查 pH 6.35 时
  `[Ag⁺]²[OH⁻]² = 10^-15.4` ✓）。原 0.5 正是上述 +14 撑出的过沉淀量。
- **converg dump 的 `ms` 不可用于逐例判读**：含 gen-2 GC 停顿，单例可
  被放大数千倍（`1 HCl+NaOH(1:1)` dump 记 5746→7776 ms，单测 **1.5 ms**）。
  逐例判读必须用 `iters`（`bench.py` 已用 repeats+中位数处理）。
  本轮"纯性能 1182 例"即此噪声，实际只有 14 例迭代数变化。
- **收敛质量口径在报假警（见 architecture.md §7 I）**：三个具名难题里
  **TS04 不成立**——它的 `resid_live = 17.975` 全部来自 meta
  `slow: True` 的**幻影硫酸盐通道**（couples 里 `SO₄²⁻/S₂O₃²⁻` 的
  `closed_with_red` 已动力学封闭），walk 从未打算执行；断言一直通过。
  同类假警还有膜封锁的金属-水通道（E24 156.12）与**痕量方向**
  （D38 33.4 来自 PbO₂ 2.9e-06 / Cr³⁺ 1.9e-06 mol——`|S|` 大 ≠ 会反应）。
  新口径：`resid_live = max|S| over 两侧在场 ∧ ¬frozen ∧ ¬slow ∧
  ¬blocked ∧ ext_max ≥ ANN_MIN_EXTENT`（`ANN_MIN_EXTENT` 是引擎自己
  的显著程度判据，slow 标注同用）；`disabled` 仍计入（J06 型真实病灶）。
  四类被排除者的 max|S| 单列 `resid_frozen/slow/blocked/trace`——
  **不统计但必须看得见**，防止口径变遮羞布。
  ```
                     旧口径      新口径
  resid_p90          1.596  →   0.064    (25×)
  resid_max        159.408  →  10.246
  n(|S|>1)            143   →    74
  iters_total       12651   = 12651     digest 逐位不变（口径不动引擎）
  ```
  ⟹ **旧口径下 77% 的"欠收敛"是假警**；此前基于 `resid_live` 的取舍
  结论（"深水区 91 例"、"resid 债"）须重估。
- **三个具名难题结算**：**H46 已解**；**TS04 不成立**（假警）；
  **J06 未解**（在分支 `rework/ph-continuity`，需 ④ 完备性判据一起
  验收）。剩 **74 例真实 `|S|>1`** 是收敛债务真实规模：磷酸银/磷酸钙
  （Y03/DR16 10.25、N23/U12 8.28）、Se33、Zn-FeSO₄、Fe-水族（6.18）。
- **施工顺序 ② 完成**：`converg-baseline.json` 按新口径重采（旧文件是
  更早 worktree 快照，digest `2b9c67b8` 与 HEAD 不符）。
- **相变微步：一个隐式契约失效 + 一处判据漏写**（分支
  `rework/phase-microstep`，**未验收**；见 architecture.md §7 J）。
  ① `solve_extent` 的 `micro_rel` 提前止损在 `hi ≤ 1e-4` 时 `return lo`，
  而 `hi` 是从 `x_max` 折半下压的——根在 1e-5 量级时 `lo` 仍是 **0.0**。
  注释辩护"返回值不被消费"是**无断言保护的隐式契约**；一旦有消费者要用
  它执行一次，拿到的是"执行 0 摩尔"。② 微步执行判据只看
  `kind in ("dissolve","precip")`，把 derived 的溶度积通道（化学上同样是
  溶解/沉淀，只是 Hess 合并写法）当质子微步跳过——**redox 必须排除**
  （纳入会放出 Ag+CuSO₄、PbS+盐酸幻影反应，断言 2→10）。
  ```
  resid_p90  0.064 → 0.008     n(|S|>1)  74 → 65     ms_mean 42.08 → 31.66 (−25%)
  resid_max 10.246 → 8.326     n_gt100  90 → 51     iters 12651 → 12976
  ```
  N23/U12 的 Ca²⁺ `2.14e-06 → 3.33e-05`（与独立手算 3.26e-05 相符）、
  E36/F15/碳酸盐族等十余例余量清零。**代价：6 例断言翻红**，其中
  N30 FeCl₃+KSCN 的 pH **1.45 → −0.46** 化学上荒谬。
  根因假设：**棘轮**——`disabled` 的签名在 `_exec` **之前**取，候选自己
  的一步就让它失效 ⟹ 重新入选再执行。真端点执行后 S≈0 故历来无害，
  derived 通道受 pH 反馈会逐步棘轮。停在分支待解。
- **新工具**：`tools/extent.py`（并列打印"走步眼里的 ext"与"独立求解的
  x*"，两者不一致即隐式契约失效）、`tools/topres.py`（残差排行含口径
  分类明细）。
- **"charge_pH 替换 pH 机器"证伪**（`tools/phdiag.py` 全量对照，
  1187 例）：76%（902）本就逐位一致；215 例分歧**全部**落在两可解释
  类且都是"机器对、电荷平衡错"——①强酸条件 `c_H` 的无阴离子记账
  ②Ksp/气相储库控 pH（账本溶解量与电荷平衡本就不自洽）。
  作废一处 API 缺陷：`charge_pH` 原把 `H_excess` 加进固定电荷 =
  双重记账（J06 报 6.216、真值 3.000）。教训：不变量 `Σz·n+He+c_H=0`
  是"账本已守恒"的结论，不能反向当输入注入方程。

0.4.2 —— **深水区迭代：社区级 pH 一致化联立**（"一步数学"对搜索
的替代扩展）：①闭式 pH 快路径（无弱组分体系的 pH 直解——bit 级
等价，digest 零位移，沉淀族实测 −5~15%）；②走步画像探针扩展
（联立尝试/冻结事件/实质微步计数 + CHEM_TRACE_WINDOWS 窗口净移/
毛周转标定——iters ≈ micro_steps + 1 确证降时长唯一路径是跳过
微步爬行）；③触发点③：社区口径（全活性含近平衡耦合约束）+
pH 提升为联立变量（S 行对 pH 仿射，快照 pH 机器的分支跳变离开
Jacobian）+ pH 自洽闭合行（pH 机器锚定联立解）+ 质子族闸（≥2
个 pKa 族 = pH 闭包启发式 → 硬失败不跳——E35 型假不动点一枪跳
0.589 mol 凌驾仲裁的防御）；④迭代 C：周转检测器冻结范围升级为
整个活性社区（跨拼写族级止震）。boundary 判定保留（Ag32 张力
族提前冻结仲裁 ~1786→101 步依赖它）。第 7 次负结果入档：netkey
质子化归一（跨侧族级抵消）折叠族内质子级（碳酸盐/磷酸/硫化物
73 例翻案——HCO₃⁻→H₂CO₃ 是真实化学而非拼写变体）。全量差分：
5 例位移全部定向向好（V25 resid_live 1.20→0.0、N34 0.084→0.009
且 162.7→70.2 ms、Ag32 0.023→0.009 且 −26%、Hg2_1 0.018→0.0
且 −14%、Co31 轨迹位移 pH 不变）；6 种子 digest 恒定；bench
3 遍均值 14.4 ms（+1.4%，张力族失败尝试成本）。H46 的 m=3 社区
联立解剖（resid 0.34 卡点：活性集含 Hess 派生行而基平衡已冻结
——Jacobian 秩亏 + 数据张力）与 J06/TS04 resid 债（17.98/6.04）
留给 0.5.x 真解酸碱专门版本。

0.4.1 —— **pH 机器呈现层自洽闸**（第 6 次试探后首次正结果）：欠收敛
walk 的 idle 残余幻影碱（恰落 −1e-3 强碱分支界，FP 尘埃级）不再
被字面读成 pH 11——呈现层阳离子-氢氧化物过饱和检查（在账阳离子
在 [OH⁻]=−He/V 下的离子积超 Ksp ≥3 个数量级 = 热力学不可能共存；
真实碱体系走步已沉淀至 Q≈Ksp 天然通过），命中则 pH 回落分支 4
（缓冲/水解主导）；先过缓冲滴定镜像分支逻辑（可吸收的碱是缓冲
化学非幻影，SE03 的 NH₄⁺ Henderson 原样放行）。走步机器零接触
（轨迹 bit 级不变，RX13 物种量逐位复现）。全量差分 31 例同签名
pH 11→4.15-7.0 诚实化（计量沉淀族 pH 7、置换放氢族 Cu²⁺ 水解
5.81、AgNO₃/Hg(NO₃)₂ 置换族 4.15-4.68；resid_live 19 例净化
>2：RX13 21.6→2.7、E23 13.7→0.5）；判定层零位移；N22 断言
翻案（旧 OH⁻ 形态骑在幻影 pH 11 上，HCl 化学计量终态中性 →
H⁺ 形态，产量断言不变）。第 6 次负结果入档：He 拆源影子账本的
走步机器作用域（30+ FAIL——水供质子族负 He 是真实碱度、幻影碱
闸门三重承重、He 标量历史真假混杂不可事后分解）。1247/1247。

0.4.0 —— **弱配形态池语义 + Zn-Cl/Ni-Cl 入库 + 走步稳定性**：
顺延三轮的权威逐级常数入库（Zn-Cl β₁–β₄ = 0.43/0.6/0.5/0.2、
Ni-Cl β₁/β₂ = 0.4/0.96，NIST46.3/PHREEQC/LibreTexts 三方交叉，
ΔHf Hess 派生；NiCl₃⁻/NiCl₄²⁻ 稀溶液无权威值维持拒录）；
**形态池三层语义**（弱配族 logβ 全部 <1.0 且族条目 ≥2 ⟹ 池折叠，
以 center 为锚）：A1 changed 池中心守恒口径（池内再分布不计事件，
NR19 盐混合 78/22 分布不再制造 changed=True 幻影）、净方程折叠 +
配体摄入抵扣（元素守恒配套，T105 的 10Cl⁻ 旁观者消除——教科书形式
2H⁺+Zn→H₂+Zn²⁺ 回归）、has 断言池聚合（O02 的 Zn²⁺ 0.95 是池
口径，free 0.74 是形态细节；has_not/has_range 维持物种级）；单
条目族（Cu-Cl/Cu-Br β₄-only）不折叠——条件性事件配合物（FeCl₃
刻蚀铜 → [CuCl₄]²⁻ 族六例锁定）。**走步稳定性两修**：pH 悬崖乒乓
冻结（第五检测器：跨键 H⁺ 喂食循环 He 符号交替 ≥2/3 且贴 ±1e-3
悬崖带 ⟹ 冻结喂食步键——O02 的 Zn(OH)₂/H⁺ ↔ Fe/H⁺ 乒乓 424→71
步，Fe 0.66→0.99，resid 6.27→0.008；E23 教训收窄：只冻 H 步键 +
悬崖带幅度 ≤3e-3 区分真实缓冲震荡）、外推守恒守卫（crawl-jump
几何外推对净减物种降倍率到线性域——N34 曾把 Zn-Cl 池成员推负后
钳零，Zn 总量凭空 +0.26、净方程 16091 OH⁻；修复后守恒逐元素归零、
C₂O₄²⁻+Zn²⁺→ZnC₂O₄ 精确命中）。性能：均值 12.9→14.2 ms（六新
物种的枚举/平衡网络成本，+10%）、>500ms 维持 0、O02/RX13 深水
例 −79%/−44%；6 seed digest 恒定（2e08106f）；1247/1247。

0.3.10 —— **pH 端点试探负结果 + 缓冲滴定堆缓存**：pH 端点连续化
（分支 4 计入残余 He 的 [H+]/[OH-] 贡献，J06 悬崖的正道尝试）被 1247
差分否决（15 例翻案：Al 系 pH 断言按旧口径定标、Fe+水痕量腐蚀升格
changed、难溶氧化物假方程）——1e-3 阈值是教科书近似口径的组成部分，
光滑化 = 0.4.x 三件套（pH 断言重审 + 痕量闸门 + 氧化物语义保护），
负结果与地图入档 architecture.md §7（第 5 次）；`_buffer_titration`
solve_extent 级堆条目缓存（bit 级等价：缓存/无缓存 200 例 0 差异、
converg digest fb3c8a93 逐位一致）：H46/X10 单例 −43~45%、全量
>50ms 66→65。

0.3.9 —— **跨进程确定性 + 枚举热路径 34%**：修复两处 set 迭代序非确定
性（candidates.py 残留字典插入序随 PYTHONHASHSEED 变化→金属守衡浮点
累加序翻转；engine.py `T.gases` set 迭代序→逸出/产物呈现序翻转；N08/
H25/UO02 三例曾跨进程 digest 掷骰子，6 个随机 seed 验证修复后恒定）；
派生候选过滤的元素位预筛（逐侧元素掩码 AND，典型 pset 幸存 ~1%，
5000 组随机 pset 等价性验证 bit 级一致）：均值 19.7→13.0 ms（−34%）、
>50 ms 92→66 例、>10 ms 682→357 例、最值 344→269 ms。Zn-Cl/Ni-Cl
权威逐级常数检索完成（NIST46.3/PHREEQC/LibreTexts 三方交叉），
入库试探被语义层缺口否决如实入档（弱配形态池聚合 = 0.4.0 立项，
architecture.md §7）。

0.3.8 —— **联立求解与收敛质量制度化**：深水区（91 例 >50ms 与 J06 型
欠收敛）双管齐下：联立 Newton 求解器（joint.py）+ 物种级周转冻结仲裁
（Ag32 型数据张力爬行 1786→101 步）+ 收敛质量基准工具（converg.py，
动 walk 语义前的全量差分基线——本版触发点②即被差分否决后回退，J06
欠收敛根因查明为 pH 滴定端点悬崖并如实记录）；净方程大系数美化
（平衡子空间投影 + 有理重构，N10 → 教科书形式）；性能：均值持平
（19.7 ms），最值 541→348 ms、>500 ms 归零、Ag32 20×、E23 29×；
1218 用例 1247 断言全绿（seed 0/5/9）。

0.3.7 —— **数据/判定语义正交 + 判定口径重构**：`changed` 判定改净反应
事件量口径（三通道：溶液相重分布 ν̄ 折扣 / 新相（固相产物、逸出）不
折扣 / 中和步显著度）——v0.3.6 曾因物种账本差被配位计量系数放大（幻影
changed=True）而撤回真实弱卤配合物数据，方向错误，本版归还语义债：
[PbCl₃]⁻ 与 [ZnI₄]²⁻ 恢复入库，[AgI₃]²⁻/[ZnCl₄]²⁻/[NiCl₄]²⁻ 以
反例形式如实记录拒录理由（architecture.md §7：高离子强度条件常数
外推失真、β4-only 与逐级系列失真——待权威逐级常数后补全）；引擎
代码结构：`_apply_override`/`_finalize_result` 从 654 行 judge() 提出
（bit 级差分验证）；性能维持并略升（均值 20.3→18.7 ms）；新增 DB11–
DB13 用例（PbCl₂ 浓盐酸氯配溶解 / AgNO₃+5KI 痕量配位锚点 / 铅氯
沉淀），1218 用例 1247 断言；发现并如实记录 PbCl₂@363K 耦合欠收敛
（Q=0.26·Ksp 出场，v0.3.8 联立求解计划）。

0.3.6 —— **Engine 统一入口 + 稳态性能重构 + 数据库权威扩充**：
`chemkit.Engine` 对象（judge/react/system 平级方法，消除 react()→System()
→judge() 层层包装；react 直落求解管线零中间对象）；删除磁盘缓存设计
（persist.py，冷启动优化与"长期运行"主用途错位，剑走偏锋——内存缓存
已覆盖稳态路径）；性能（均值 23.1→20.3 ms、>50 ms 119→91：微步二分
早停 / 微步排空 / 枚举静态段 present 二级缓存 / 闭包外提，全量差分
sha256 比对 bit 级一致）；数据库扩充（β 配合物 150→157：Cu(I) 氨/氰/
碘/硫氰配族、Co(SCN)₄ 钴蓝、Cd(OH)₄；Ksp 224→230：AgSCN（Volhard）、
SrCrO₄、Ba(IO₃)₂、KClO₄、Zn₃(PO₄)₂；电对 +2（[Cu(NH₃)₂]⁺/Cu、
[Cu(CN)₂]⁻/Cu）；thermo +9 标准态元素——金属置换热闭环（Fe+CuSO₄
153.87 kJ/mol 教科书值精确命中））；**配对闸门语义修复**（vs_red_E_max
改用电对自身 E0 而非物种级最低值——配合物电对曾把裸金属的还原电位
串扰拉破 N₂O/NO 动力学闸门）；10 条新化学用例（铜氨溶液/钴蓝/Volhard/
钡锶分离等）+ 2 条按权威 Ksp 修正（X33/X42 AgSCN 化学修正），1215 用例
1244 断言通道。

0.3.5 —— 用户侧性能重构 + 热数据统一：**零第三方依赖**（手写免分数
整数消元零空间取代 sympy，33401 次调用差分验证 bit 级一致；import
1.5 s→30 ms）；thermo 双源冲突重构（thermo.json 单一事实源 + 逸出
气相拆分）；数据库扩充 28 条 ΔHf（BaF₂/ZnCO₃/HgI₂/Cu₂O 等双源验证）。

0.3.4 —— 绝热耦合真变温（isothermal 默认 True；False 时平衡⇌能量外层
温度不动点）；性能（慢标注节奏记忆/爬行窗口外推/热路径查表合并）；
模块拆分（engine 3246→1030 行四层拆出，单向依赖链）。

0.3.3 —— 性能重构（四级缓存体系，全套 2 min→36 s）；用户侧 API 统一
门面 `chemkit.interfaces`；README 性能章节。

0.3.1 —— 接口定名（changed/reacted/degree/...），元素周期表级拆盐兜底，
镧系/锕系/铂族/镭数据库扩充（224 Ksp / 161 电对 / 150 logβ），净离子
方程式单一数据源，1200 条测试全通过。
